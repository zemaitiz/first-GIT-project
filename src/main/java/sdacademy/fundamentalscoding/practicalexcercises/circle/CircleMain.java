/*

package main.sdacademy.fundamentalscoding.practicalexcercises.circle;

import java.util.Scanner;

public class CircleMain {
    public static final double PI = Math.PI;
    Scanner input = new Scanner(System.in);

    public double calculateLength(double inputRadius) {
        return 2 * PI * inputRadius;
    }
    public double setRadius (double radius) {
        System.out.println("Enter the radius:");
        double inputRadius = input.nextDouble();
        return inputRadius;
    }
    public String setColor (String color) {
        System.out.println("Enter the color:");
        String inputColor = input.nextLine();
        return inputColor;
    }

    public static void main(String[] args) {
       */
/* System.out.println("Enter the color:");
        String inputColor = input.nextLine();

        System.out.println("Enter the radius:");
        double inputRadius = input.nextDouble();*//*





        Circle someCircle = new Circle;

        //double circleLength = 2 * PI * someCircle.getRadius();
        //double circleArea = PI * Math.pow(someCircle.getRadius(), 2);

        //System.out.println("The circle is " + someCircle.getColor() + " and has a radius of " + someCircle.getRadius() + "its length is " + circleLength +" and its area is " + circleArea);
        System.out.println("Circle length is " + someCircle.calculateLength(inputRadius));
        System.out.println("Circle area is " + someCircle.calculateArea(inputRadius));
        System.out.println("Circle color is " + someCircle.printColor(inputColor));
    }


}
*/
